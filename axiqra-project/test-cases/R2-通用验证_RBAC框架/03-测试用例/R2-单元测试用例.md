# R2 单元测试用例

> **Round：** R2 — 通用验证 + RBAC 框架
> **编写时间：** 2026-06-08
> **测试类型：** 单元测试

---

## 1. NoHtmlValidator 测试用例

| 用例编号 | TC-R2-VAL-001 |
|----------|----------------|
| 名称 | nullValue_returnsTrue |
| 描述 | null 输入返回 true（JSR-380 约定，null 值视为合法） |
| 前置条件 | 无 |
| 输入 | `null` |
| 预期输出 | `true` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-002 |
|----------|----------------|
| 名称 | blankValue_returnsTrue |
| 描述 | 空白字符串返回 true |
| 前置条件 | 无 |
| 输入 | `"   "` |
| 预期输出 | `true` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-003 |
|----------|----------------|
| 名称 | tooLong_returnsFalse |
| 描述 | 超过 500 字符直接拒绝，无需进一步解析 |
| 前置条件 | 无 |
| 输入 | `"a".repeat(501)` |
| 预期输出 | `false` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-004 |
|----------|----------------|
| 名称 | dangerousInput_returnsFalse（参数化 5 条） |
| 描述 | 危险标签和协议（`<script>`、`<img>`、`<svg>`、javascript:、data:）应返回 false |
| 前置条件 | 无 |
| 输入 | `<script>alert(1)</script>`、`<img src=x onerror=alert(1)>`、`javascript:alert(1)`、`data:text/html,<h1>test</h1>`、`<svg onload=alert(1)>` |
| 预期输出 | `false` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-005 |
|----------|----------------|
| 名称 | safeInput_returnsTrue（参数化） |
| 描述 | 普通文本应通过验证 |
| 前置条件 | 无 |
| 输入 | `"hello world"`、`"user_123"`、`"中文昵称"`、长安全文本 |
| 预期输出 | `true` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-006 |
|----------|----------------|
| 名称 | namedEntityLessThanScript_returnsFalse |
| 描述 | 命名实体 `&lt;script&gt;` 解码后应被 `<script>` 拦截规则捕获 |
| 前置条件 | 无 |
| 输入 | `&lt;script&gt;alert(1)&lt;/script&gt;` |
| 预期输出 | `false` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-007 |
|----------|----------------|
| 名称 | hexNumericEntityScript_returnsFalse |
| 描述 | 十六进制数字实体解码后应被拦截 |
| 前置条件 | 无 |
| 输入 | `&#60;script&#62;alert(1)&#60;/script&#62;` |
| 预期输出 | `false` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-008 |
|----------|----------------|
| 名称 | decimalNumericEntityScript_returnsFalse |
| 描述 | 十进制数字实体解码后应被拦截 |
| 前置条件 | 无 |
| 输入 | `&#60;script&#62;alert(1)&#60;/script&#62;` |
| 预期输出 | `false` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-009 |
|----------|----------------|
| 名称 | mixedCaseHexEntity_returnsFalse |
| 描述 | 混合大小写十六进制实体（&#X3C;）应被拦截 |
| 前置条件 | 无 |
| 输入 | `&#X3C;SCRIPT&#X3E;alert(1)&#X3C;/SCRIPT&#X3E;` |
| 预期输出 | `false` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-010 |
|----------|----------------|
| 名称 | safeNamedEntities_returnsTrue |
| 描述 | 无害命名实体（&、&quot;）不应触发误报 |
| 前置条件 | 无 |
| 输入 | `Tom & Jerry &quot;friends&quot;` |
| 预期输出 | `true` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-011 |
|----------|----------------|
| 名称 | ampersandInNormalText_returnsTrue |
| 描述 | 普通文本中的 & 字符不应触发误报 |
| 前置条件 | 无 |
| 输入 | `"A & B"` |
| 预期输出 | `true` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-012 |
|----------|----------------|
| 名称 | entityWithoutSemicolon_returnsTrue |
| 描述 | 不带分号的实体（`&name`）不应被解码，直接放行 |
| 前置条件 | 无 |
| 输入 | `"Tom & Jerry"` |
| 预期输出 | `true` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-013 |
|----------|----------------|
| 名称 | outOfRangeCodePoint_returnsTrue |
| 描述 | 超出 Unicode 范围的码点（如 `&#x110000;`）不崩溃，返回 true |
| 前置条件 | 无 |
| 输入 | `"&#x110000;"` |
| 预期输出 | `true` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-014 |
|----------|----------------|
| 名称 | largeSafeInput_returnsTrue |
| 描述 | 大于 400 字符的安全输入性能测试 |
| 前置条件 | 无 |
| 输入 | `"hello world".repeat(40)` |
| 预期输出 | `true` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-015 |
|----------|----------------|
| 名称 | largeDangerousInput_returnsFalse |
| 描述 | 大于 400 字符的危险输入应被拒绝 |
| 前置条件 | 无 |
| 输入 | `"<script>".repeat(51)` |
| 预期输出 | `false` |
| 测试类 | `NoHtmlValidatorTest` |

| 用例编号 | TC-R2-VAL-016 |
|----------|----------------|
| 名称 | veryLargeInput_rejectedAtLengthCheck |
| 描述 | 大于 1KB 输入在长度检查阶段快速拒绝 |
| 前置条件 | 无 |
| 输入 | `"x".repeat(2048)` |
| 预期输出 | `false` |
| 测试类 | `NoHtmlValidatorTest` |

---

## 2. WorkspaceServiceImpl 测试用例

### 2.1 listMyWorkspaces

| 用例编号 | TC-R2-WS-LIST-001 |
|----------|-------------------|
| 名称 | userId 为 null 应返回空分页 |
| 描述 | `listMyWorkspaces(null)` 应返回空列表 |
| 前置条件 | 无 |
| 输入 | `null` |
| 预期输出 | `PageResponse.records` 为空列表 |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-LIST-002 |
|----------|-------------------|
| 名称 | 仅个人空间时返回 owner 的空间 |
| 描述 | 用户仅有自己创建的空间时，列表应包含该空间，角色为 owner |
| 前置条件 | 无 |
| 输入 | USER_ID 有 1 个 workspace（owner） |
| 预期输出 | `total=1`，第一条 `myRole=owner` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-LIST-003 |
|----------|-------------------|
| 名称 | 仅成员空间时返回成员空间 |
| 描述 | 用户非 owner 但为某空间成员时，应在列表中体现 |
| 前置条件 | 无 |
| 输入 | USER_ID 为某 workspace 的 member |
| 预期输出 | `total=1`，`myRole=member` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-LIST-004 |
|----------|-------------------|
| 名称 | owner 空间应在 member 空间之前排序 |
| 描述 | 列表排序：owner 空间优先于 member 空间 |
| 前置条件 | 无 |
| 输入 | USER_ID 同时有 owner 和 member 空间 |
| 预期输出 | 第 1 条 `myRole=owner`，第 2 条 `myRole=member` |
| 测试类 | `WorkspaceServiceImplTest` |

### 2.2 create

| 用例编号 | TC-R2-WS-CRT-001 |
|----------|-------------------|
| 名称 | userId 为 null 应抛参数异常 |
| 描述 | 创建 workspace 时 userId 为 null 抛 BizException |
| 前置条件 | 无 |
| 输入 | `create(null, "personal", "My Space")` |
| 预期输出 | `BizException(code=PARAM_INVALID)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-CRT-002 |
|----------|-------------------|
| 名称 | 无效 workspaceType 应抛参数异常 |
| 描述 | 不支持的类型字符串应被拒绝 |
| 前置条件 | 无 |
| 输入 | `create(USER_ID, "invalid_type", "My Space")` |
| 预期输出 | `BizException(code=PARAM_INVALID)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-CRT-003 |
|----------|-------------------|
| 名称 | 同名空间应抛重复异常 |
| 描述 | 同 owner 下同名 workspace 应拒绝 |
| 前置条件 | 无 |
| 输入 | `create(USER_ID, "personal", "My Space")`，其中已有同名空间 |
| 预期输出 | `BizException(code=DUPLICATE_ENTRY)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-CRT-004 |
|----------|-------------------|
| 名称 | 非个人空间且 name 为空应抛参数异常 |
| 描述 | team 类型必须提供 workspaceName |
| 前置条件 | 无 |
| 输入 | `create(USER_ID, "team", null)` |
| 预期输出 | `BizException(code=PARAM_INVALID)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-CRT-005 |
|----------|-------------------|
| 名称 | 个人空间 name 为空应自动使用 username |
| 描述 | personal 类型 name 为空时自动生成"username 的空间" |
| 前置条件 | 无 |
| 输入 | `create(USER_ID, "personal", null)`，用户名为 alice |
| 预期输出 | `workspaceName="alice 的空间"` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-CRT-006 |
|----------|-------------------|
| 名称 | 正常创建应插入 workspace 和 membership |
| 描述 | 成功创建后 workspace 和 owner membership 均应写入 DB |
| 前置条件 | 无 |
| 输入 | 正常参数 |
| 预期输出 | workspaceId 不为空，`myRole=owner`，`memberCount=1`，`membershipMapper.insertSelective` 被调用 |
| 测试类 | `WorkspaceServiceImplTest` |

### 2.3 getById

| 用例编号 | TC-R2-WS-GET-001 |
|----------|-------------------|
| 名称 | workspaceId 为 null 应抛参数异常 |
| 前置条件 | 无 |
| 输入 | `getById(null, USER_ID)` |
| 预期输出 | `BizException(code=PARAM_INVALID)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-GET-002 |
|----------|-------------------|
| 名称 | workspace 不存在应抛资源不存在异常 |
| 前置条件 | 无 |
| 输入 | workspaceId 不存在的 ID |
| 预期输出 | `BizException(code=WORKSPACE_NOT_FOUND)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-GET-003 |
|----------|-------------------|
| 名称 | 非成员无权访问应抛禁止异常 |
| 前置条件 | 无 |
| 输入 | 用户非 workspace 成员 |
| 预期输出 | `BizException(code=FORBIDDEN)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-GET-004 |
|----------|-------------------|
| 名称 | 成员访问应返回 workspace 详情 |
| 前置条件 | 无 |
| 输入 | 成员用户访问自己的 workspace |
| 预期输出 | `id` 匹配，`myRole=member`，`memberCount` 正确 |
| 测试类 | `WorkspaceServiceImplTest` |

### 2.4 delete

| 用例编号 | TC-R2-WS-DEL-001 |
|----------|-------------------|
| 名称 | workspaceId 为 null 应抛参数异常 |
| 前置条件 | 无 |
| 输入 | `delete(null, USER_ID)` |
| 预期输出 | `BizException(code=PARAM_INVALID)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-DEL-002 |
|----------|-------------------|
| 名称 | 非 owner 无权删除应抛禁止异常 |
| 前置条件 | 无 |
| 输入 | 非 owner 用户调用 delete |
| 预期输出 | `BizException(code=FORBIDDEN)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-DEL-003 |
|----------|-------------------|
| 名称 | owner 删除应执行软删除 |
| 描述 | owner 删除 workspace 时同时软删除 workspace 和所有 active membership，membership 批量软删不复用 workspace version |
| 前置条件 | 无 |
| 输入 | owner 用户调用 delete |
| 预期输出 | `workspaceMapper.softDeleteById` 被调用，`membershipMapper.softDeleteByWorkspaceId(workspaceId, suspended)` 被调用 |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-DEL-004 |
|----------|-------------------|
| 名称 | 软删除返回 0 行应抛并发修改异常 |
| 前置条件 | 无 |
| 输入 | `softDeleteById` 返回 0（版本冲突） |
| 预期输出 | `BizException(code=CONCURRENT_MODIFICATION)` |
| 测试类 | `WorkspaceServiceImplTest` |

### 2.5 addMember

| 用例编号 | TC-R2-WS-ADD-001 |
|----------|-------------------|
| 名称 | 非 admin 无权添加成员应抛禁止异常 |
| 前置条件 | 无 |
| 输入 | member 角色用户调用 addMember |
| 预期输出 | `BizException(code=FORBIDDEN)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-ADD-002 |
|----------|-------------------|
| 名称 | 添加 owner 角色应抛禁止异常 |
| 前置条件 | 无 |
| 输入 | admin 尝试添加 owner 角色 |
| 预期输出 | `BizException(code=FORBIDDEN)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-ADD-003 |
|----------|-------------------|
| 名称 | 添加不存在的用户应抛用户不存在异常 |
| 前置条件 | 无 |
| 输入 | targetUserId 对应用户不存在 |
| 预期输出 | `BizException(code=USER_NOT_FOUND)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-ADD-004 |
|----------|-------------------|
| 名称 | 添加已有活跃成员应抛重复异常 |
| 描述 | 同一用户已是 workspace 活跃成员时拒绝重复添加 |
| 前置条件 | 无 |
| 输入 | 目标用户已以 active 状态加入该 workspace |
| 预期输出 | `BizException(code=DUPLICATE_ENTRY)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-ADD-005 |
|----------|-------------------|
| 名称 | admin 添加有效成员应成功 |
| 描述 | 有效参数应写入 membership 并返回 MemberVO |
| 前置条件 | 无 |
| 输入 | admin 添加一个已存在的非成员用户 |
| 预期输出 | `memberId` 不为空，`membershipMapper.insertSelective` 被调用 |
| 测试类 | `WorkspaceServiceImplTest` |

### 2.6 removeMember

| 用例编号 | TC-R2-WS-RMV-001 |
|----------|-------------------|
| 名称 | 不能移除 owner 应抛禁止异常 |
| 前置条件 | 无 |
| 输入 | 尝试移除 role=owner 的成员 |
| 预期输出 | `BizException(code=FORBIDDEN)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-RMV-002 |
|----------|-------------------|
| 名称 | 不能移除自己应抛禁止异常 |
| 描述 | 管理员不能通过此接口将自己从 workspace 中移除 |
| 前置条件 | 无 |
| 输入 | 管理员尝试移除自己 |
| 预期输出 | `BizException(code=FORBIDDEN)`，消息包含"不能将您自己" |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-RMV-003 |
|----------|-------------------|
| 名称 | admin 移除有效成员应成功（软删除） |
| 描述 | 移除成员应调用 `membershipMapper.softDelete()` 设置 `is_deleted=TRUE`，以符合唯一索引语义 |
| 前置条件 | 无 |
| 输入 | admin 移除一个有效成员 |
| 预期输出 | `membershipMapper.softDelete(targetMemberId, SUSPENDED)` 被调用 |
| 测试类 | `WorkspaceServiceImplTest` |

### 2.7 updateMemberRole

| 用例编号 | TC-R2-WS-UPD-001 |
|----------|-------------------|
| 名称 | 不能修改 owner 角色应抛禁止异常 |
| 前置条件 | 无 |
| 输入 | 尝试将 owner 角色改为 admin |
| 预期输出 | `BizException(code=FORBIDDEN)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-UPD-002 |
|----------|-------------------|
| 名称 | 不能将成员提升为 owner 应抛禁止异常 |
| 前置条件 | 无 |
| 输入 | 尝试将 member 提升为 owner |
| 预期输出 | `BizException(code=FORBIDDEN)` |
| 测试类 | `WorkspaceServiceImplTest` |

| 用例编号 | TC-R2-WS-UPD-003 |
|----------|-------------------|
| 名称 | owner 变更成员角色应成功 |
| 描述 | owner 可以将 member 变更为 admin |
| 前置条件 | 无 |
| 输入 | owner 将某成员 role 从 member 改为 admin |
| 预期输出 | `membershipMapper.updateRole` 被调用 |
| 测试类 | `WorkspaceServiceImplTest` |

### 2.8 update

| 用例编号 | TC-R2-WS-UPDT-001~007 |
|----------|----------------------|
| 名称 | 覆盖：null workspaceId、非 owner 更新、workspace 不存在、无效 type、同名重复、两字段均为空（不调用 DB）、正常更新 |
| 描述 | update 方法的 7 个分支场景 |
| 测试类 | `WorkspaceServiceImplTest` |

---

## 3. UserServiceImpl 测试用例

### 3.1 updateProfile

| 用例编号 | TC-R2-USR-001 |
|----------|----------------|
| 名称 | userId 为 null 应抛参数异常 |
| 前置条件 | 无 |
| 输入 | `updateProfile(null, "nick", "email@test.com", null)` |
| 预期输出 | `BizException(code=PARAM_INVALID)` |
| 测试类 | `UserServiceImplTest` |

| 用例编号 | TC-R2-USR-002 |
|----------|----------------|
| 名称 | 用户不存在应抛用户不存在异常 |
| 前置条件 | 无 |
| 输入 | 用户已被删除（selectActiveById 返回 null） |
| 预期输出 | `BizException(code=USER_NOT_FOUND)` |
| 测试类 | `UserServiceImplTest` |

| 用例编号 | TC-R2-USR-003~010 |
|----------|-------------------|
| 名称 | 覆盖：两字段均为空直接返回、email 被其他用户占用（DB 约束触发）、更新自己 email 成功、DB 返回 0 行（并发）、nickname 更新、whitespace-only nickname 忽略+email 正常、whitespace-only email 忽略+nickname 正常、DB email 约束异常翻译、avatar 更新 |
| 描述 | updateProfile 的 8 个分支场景 |
| 测试类 | `UserServiceImplTest` |

---

## 4. RbacServiceImpl 测试用例

| 用例编号 | TC-R2-RBAC-001 |
|----------|----------------|
| 名称 | hasRole 应代理到 rbacPort |
| 描述 | `hasRole(userId, workspaceId, ADMIN)` 调用后 `rbacPort.hasRole` 被调用一次 |
| 前置条件 | 无 |
| 输入 | `rbacService.hasRole(1L, 100L, ADMIN)` |
| 预期输出 | `rbacPort.hasRole` 被调用，返回 `rbacPort` 的结果 |
| 测试类 | `RbacServiceImplTest` |

| 用例编号 | TC-R2-RBAC-002~010 |
|----------|-------------------|
| 名称 | 覆盖：isOwner 代理、isAdmin 代理、getRole 返回 null/getMemberships、isMember 代理、hasScope 代理成功/失败 |
| 描述 | RbacService 对所有 Port 方法的代理验证 |
| 测试类 | `RbacServiceImplTest` |

---

## 5. NavServiceImpl 测试用例

| 用例编号 | TC-R2-NAV-001 |
|----------|----------------|
| 名称 | userId 为 null 应返回空导航 |
| 描述 | 未登录用户导航应为空（空 baseUserNav） |
| 前置条件 | 无 |
| 输入 | `getNav(null)` |
| 预期输出 | `baseUserNav` 和 `spaceMembershipNav` 均为空 |
| 测试类 | `NavServiceImplTest` |

| 用例编号 | TC-R2-NAV-002 |
|----------|----------------|
| 名称 | 登录用户应返回基础菜单 |
| 描述 | 登录用户的基础菜单应包含 home、profile、workspaces |
| 前置条件 | 无 |
| 输入 | `getNav(USER_ID)` |
| 预期输出 | `baseUserNav` 包含 id: `home`、`profile`、`workspaces` |
| 测试类 | `NavServiceImplTest` |

| 用例编号 | TC-R2-NAV-003~012 |
|----------|-------------------|
| 名称 | 覆盖：无成员空间返回空 spaceMembershipNav、admin 成员有成员管理菜单、owner 成员有成员管理菜单、viewer 成员无成员管理菜单、search scope 用户返回搜索菜单、无 scope 用户返回空 grantedScopeNav、治理菜单和管理菜单 S1 为空、memberships 返回 null 不抛异常、memberships 含 null 元素跳过、SUSPENDED 成员不出现在菜单 |
| 描述 | NavService 的菜单组装逻辑全覆盖 |
| 测试类 | `NavServiceImplTest` |

---

## 6. PolicyEngineServiceImpl 测试用例

| 用例编号 | TC-R2-POL-001 |
|----------|----------------|
| 名称 | evaluate 应返回 policyEnginePort 的结果 |
| 描述 | 策略评估直接透传 Port 结果 |
| 前置条件 | 无 |
| 输入 | 有效的 `PolicyEvaluationRequest` |
| 预期输出 | 返回 `PolicyDecision.ALLOW`，`policyCode=ABAC-READ-MEMBER` |
| 测试类 | `PolicyEngineServiceImplTest` |

| 用例编号 | TC-R2-POL-002 |
|----------|----------------|
| 名称 | hasScope 应代理到 policyEnginePort |
| 描述 | `hasScope(userId, "search:read")` 调用后 Port 被调用 |
| 测试类 | `PolicyEngineServiceImplTest` |

| 用例编号 | TC-R2-POL-003~007 |
|----------|-------------------|
| 名称 | 覆盖：enforce 允许时不抛异常、enforce 拒绝时抛 BizException FORBIDDEN、DENY_SCOPE_MISSING 转为 FORBIDDEN、DENY_RISK_LEVEL_TOO_HIGH 转为 FORBIDDEN |
| 描述 | enforce 方法的策略拒绝分支全覆盖 |
| 测试类 | `PolicyEngineServiceImplTest` |

---

## 7. WorkspaceController 接口测试用例

| 用例编号 | TC-R2-CTRL-WS-001 |
|----------|-------------------|
| 名称 | POST /api/workspaces 应调用 service.create 并返回 workspace |
| 描述 | Controller 接收请求并正确调用 Service |
| 测试类 | `WorkspaceControllerTest` |

| 用例编号 | TC-R2-CTRL-WS-002 |
|----------|-------------------|
| 名称 | DELETE /api/workspaces/{workspaceId} 应调用 service.delete |
| 测试类 | `WorkspaceControllerTest` |

| 用例编号 | TC-R2-CTRL-WS-003 |
|----------|-------------------|
| 名称 | POST /api/workspaces/{workspaceId}/members 无效角色应抛 BizException |
| 测试类 | `WorkspaceControllerTest` |

| 用例编号 | TC-R2-CTRL-WS-004 |
|----------|-------------------|
| 名称 | POST /api/workspaces/{workspaceId}/members 有效参数应调用 service.addMember |
| 测试类 | `WorkspaceControllerTest` |

| 用例编号 | TC-R2-CTRL-WS-005 |
|----------|-------------------|
| 名称 | DELETE /api/workspaces/{workspaceId}/members/{memberId} 应调用 service.removeMember |
| 测试类 | `WorkspaceControllerTest` |

| 用例编号 | TC-R2-CTRL-WS-006 |
|----------|-------------------|
| 名称 | PUT /api/workspaces/{workspaceId}/members 应调用 service.updateMemberRole |
| 测试类 | `WorkspaceControllerTest` |

---

## 8. PolicyController 接口测试用例

| 用例编号 | TC-R2-CTRL-POL-001 |
|----------|-------------------|
| 名称 | POST /api/policy/evaluate 应注入当前用户 ID 并返回评估结果 |
| 描述 | Controller 层覆盖请求中的 subjectId，防止越权 |
| 测试类 | `PolicyControllerTest` |

| 用例编号 | TC-R2-CTRL-POL-002 |
|----------|-------------------|
| 名称 | POST /api/policy/enforce 应注入当前用户 ID 并调用 service.enforce |
| 测试类 | `PolicyControllerTest` |

| 用例编号 | TC-R2-CTRL-POL-003 |
|----------|-------------------|
| 名称 | GET /api/policy/check 应返回 scope 检查结果 |
| 测试类 | `PolicyControllerTest` |

| 用例编号 | TC-R2-CTRL-POL-004 |
|----------|-------------------|
| 名称 | 无 scope 应返回 false |
| 测试类 | `PolicyControllerTest` |

---

## 用例汇总

| 模块 | 测试类 | 用例数 | 状态 |
|------|--------|--------|------|
| NoHtmlValidator | `NoHtmlValidatorTest` | 23 | ✅ |
| WorkspaceServiceImpl | `WorkspaceServiceImplTest` | 34 | ✅ |
| UserServiceImpl | `UserServiceImplTest` | 11 | ✅ |
| RbacServiceImpl | `RbacServiceImplTest` | 10 | ✅ |
| NavServiceImpl | `NavServiceImplTest` | 12 | ✅ |
| PolicyEngineServiceImpl | `PolicyEngineServiceImplTest` | 7 | ✅ |
| WorkspaceController | `WorkspaceControllerTest` | 6 | ✅ |
| PolicyController | `PolicyControllerTest` | 4 | ✅ |
| **总计** | **8 个测试类** | **107** | **107 ✅** |
