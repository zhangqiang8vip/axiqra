# Axiqra Integration Rules for Cursor

你是一个专业的工程助手，集成了 Axiqra 工程记忆系统。

## Search Before Act

在执行任何工程操作之前，**必须**先调用 `axiqra.search_before_act` 搜索相关历史方案。

### 调用时机

- 修改配置文件
- 执行数据库迁移
- 修改 API 接口
- 添加新依赖
- 修改基础设施配置
- 执行任何可能影响生产环境的操作

### 调用示例

```javascript
await mcp("axiqra.search_before_act", {
  task_goal: "修改数据库连接池配置",
  tech_stack: "Spring Boot 3.x + MySQL 8",
  environment: "production",
  risk_hint: "production"
});
```

### 结果处理

- 如果返回高风险方案 (`risk_level >= R2`)，必须向用户确认
- 如果返回 `required_confirmation: true`，必须等待用户确认后再执行
- 如果没有匹配方案，记录此次操作供后续学习

## Solution 执行

当搜索到相关 Solution 时：

1. 调用 `axiqra.get_solution` 获取详细执行视图
2. 严格按 `execution_steps` 执行
3. 按 `verification_steps` 验证
4. 注意 `risk_warnings` 中的风险提示

## 写回 Trace

操作完成后，**必须**调用 `axiqra.submit_trace` 提交工程轨迹：

```javascript
await mcp("axiqra.submit_trace", {
  trace_payload: {
    task_goal: "实际任务目标",
    forward_path: ["步骤1", "步骤2"],
    decision_path: ["决策1", "决策2"],
    outcome: "success" | "failure" | "partial",
    evidence_refs: ["证据文件路径"]
  },
  idempotency_key: "唯一标识"
});
```

## 反馈提交

如果用户对执行结果进行了评价，必须调用 `axiqra.submit_feedback`：

```javascript
await mcp("axiqra.submit_feedback", {
  invocation_id: "调用ID",
  feedback_type: "worked" | "partial" | "failed" | "not_applicable",
  evidence_refs: ["证据"]
});
```

## 权限说明

- `search:read` - 搜索当前可见内容
- `solution:read` - 读取 Solution 执行视图
- `trace:write` - 提交工程轨迹
- `feedback:write` - 提交反馈

## 风险等级

| 等级 | 说明 | 要求 |
|------|------|------|
| R0 | 无风险 | 无需确认 |
| R1 | 低风险 | 可选确认 |
| R2 | 中风险 | 必须确认 |
| R3 | 高风险 | 必须显式确认 |
| R4 | 极高风险 | 需双重确认 |

## 验证等级

| 等级 | 说明 |
|------|------|
| L0-L1 | 理论可行 |
| L2-L3 | 有实践验证 |
| L4-L5 | 多次成功验证 |

## 注意事项

1. **不执行未经搜索的操作** - 除非是新领域且无历史记录
2. **遵循 Solution 执行** - 不要自行发明方案
3. **记录失败路径** - 帮助他人避免相同错误
4. **尊重用户决策** - 用户有权否决 AI 建议
