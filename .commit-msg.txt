fix: logout() only swallow NotLoginException for idempotency

Per CodeRabbit review: the previous implementation swallowed all exceptions
from StpUtil.getLoginIdAsLong() and StpUtil.logout(), masking real failures
from clients. Now only NotLoginException is caught (the idempotent case); any
other unexpected exception propagates to GlobalExceptionHandler so clients get
proper error feedback.
