# Axiqra 文档与研发进度补充说明

更新时间：2026-06-10

## 当前分支与版本状态

- `main` 已同步到远程 `origin/main`
- `R4` 已完成并合入 `main`（PR #25）
- 本地 `R4` 分支已删除
- `R5` 为下一阶段工作分支，用于承接 Search + Solution 模块开发

## R4 实际完成情况

R4 已完成以下闭环，并已进入主干：

- Quota：每日配额控制，超限返回 `429` 与明确错误语义
- RateLimit：分钟级限流与 `retry_after` 返回
- Connect：会话创建、doctor 检测、状态流转与持久化适配
- MCP / CLI：S1 核心闭环样例、协议实现与测试样例已进入仓库

## R5 启动说明

R5 聚焦两块能力：

- Search：权限预过滤、多路召回、排序融合、Candidate Seed、空结果处理
- Solution：详情与版本、验证等级、风险等级、状态机与反馈统计

建议 R5 从 `main` 拉出独立分支推进，并保持以下顺序：

1. 先完成 Search 的接口边界、权限过滤与召回骨架
2. 再补 Solution 的状态机、详情与版本聚合
3. 最后补测试样例、验收证据与文档闭环

## 计划文件同步

已同步更新 `.cursor/plans/axiqra_s1_mvp_最小原型_dd8130f3.plan.md`：

- 将 `R4` 标记为已完成
- 将 `R5` 标记为进行中
- 补充 `R4` 已合入 `main` 的说明
- 标注 `R5` 已启动的阶段说明
