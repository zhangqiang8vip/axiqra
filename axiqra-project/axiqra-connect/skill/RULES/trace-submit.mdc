# Axiqra 轨迹提交规则

> **触发时机**：AI Agent 完成工程任务后必须提交 Engineering Trace Package

## 什么是 Engineering Trace Package

工程轨迹包是 AI 完成一次工程任务后的完整过程记录，包含：

### 1. 正向路径
- 目标
- 上下文
- 方案选择
- 执行步骤
- 修改内容
- 验证结果

### 2. 反向路径
- 异常/失败
- 根因分析
- 修复过程
- 回滚步骤（如有）

### 3. 决策路径
- 为什么选这个方案
- 为什么不用另一个方案
- 适用边界

### 4. 证据路径
- 日志
- diff
- 测试结果
- 截图
- 命令输出

### 5. 回滚路径（如有）
- 失败后如何恢复
- 降级方案

## 提交时机

### 必须提交

- 任务成功完成
- 任务失败但找到解决方案
- 用户明确要求记录

### 可选提交

- 发现新问题（未在方案库中）
- 有价值的失败经验

### 禁止提交

- 纯测试/实验性代码
- 未经验证的方案
- 包含敏感信息的轨迹

## 脱敏规则

**必须脱敏的内容**：

| 敏感类型 | 占位符 |
|---------|--------|
| API Keys | `<TOKEN_REDACTED>` |
| 密码 | `<PASSWORD_REDACTED>` |
| 客户名 | `<CUSTOMER_REDACTED>` |
| 内网 IP | `<INTERNAL_IP>` |
| 私有域名 | `<PRIVATE_DOMAIN>` |
| 私有路径 | `<PRIVATE_PATH_REDACTED>` |
| 私有仓库 | `<PRIVATE_REPO>` |

**禁止提交的敏感信息**：

- ❌ 真实 API Keys / Tokens
- ❌ 密码 / 密钥
- ❌ 客户真实数据
- ❌ 内网拓扑信息
- ❌ 私有代码库路径
- ❌ 认证凭据

## 轨迹格式

```json
{
  "trace": {
    "session_id": "唯一标识",
    "task_goal": "任务目标",
    "environment": {
      "tech_stack": "技术栈",
      "version": "版本",
      "os": "操作系统"
    },
    "forward_path": [
      {
        "step": 1,
        "action": "执行动作",
        "reason": "为什么这样做",
        "result": "执行结果"
      }
    ],
    "reverse_path": [
      {
        "attempted": "尝试的方案",
        "reason_failed": "失败原因",
        "evidence": "证据"
      }
    ],
    "decision_path": [
      {
        "decision": "决策点",
        "options_considered": ["考虑的选项"],
        "selected": "最终选择",
        "reason": "选择理由"
      }
    ],
    "evidence_refs": [
      "日志引用",
      "diff 引用",
      "测试输出引用"
    ],
    "rollback_path": {
      "enabled": true,
      "steps": ["回滚步骤"]
    },
    "outcome": "success|failure|partial",
    "user_confirmation": "confirmed|pending|rejected"
  }
}
```

## CLI 提交命令

```bash
# 提交轨迹
axiqra trace submit ./trace.json

# 提交并标记
axiqra trace submit ./trace.json --tag bug-fix --tag production

# 查看待提交轨迹
axiqra trace list --status pending

# 查看轨迹详情
axiqra trace get TRACE-xxx
```

## 状态流转

```
draft → prechecked → user_confirmed → submitted → project_case_created
                  ↓
              rejected（用户拒绝）
```

## 质量要求

### 完整度

- ✅ 有明确的任务目标
- ✅ 有技术栈和环境描述
- ✅ 有执行步骤
- ✅ 有验证结果
- ✅ 有决策理由

### 低质量轨迹特征

- ❌ 只有代码 diff，没有上下文
- ❌ 只有"已解决"，没有过程
- ❌ 缺少验证证据
- ❌ 没有适用边界说明

## 与 Case 的关系

```
Engineering Trace Package
        ↓
    Case Package（提交格式）
        ↓
    Project Case（沉淀资产）
        ↓
    Public Case（脱敏公开）
        ↓
    Solution（标准方案）
```

## 关联规则

- `search-before-act.mdc` - 行动前搜索规则
- `feedback.mdc` - 反馈规则
