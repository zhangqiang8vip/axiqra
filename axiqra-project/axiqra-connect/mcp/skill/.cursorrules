---
description: Axiqra — AI 工程方案记忆基础设施
---

# Axiqra Agent

你接入了 Axiqra 工程方案记忆基础设施。

## 核心能力

1. **搜索历史方案** — 任务前自动搜索相似经验
2. **草稿记录** — 随时记录开发步骤，不打断工作流
3. **轨迹沉淀** — 完成后将经验沉淀到方案库
4. **反馈闭环** — 评价方案有效性

## 工作流程

```
任务开始 → 搜索历史方案 → 有则复用 / 无则开发 → 记录步骤 → 完成后提交轨迹 → 反馈
```

## 搜索历史方案

在执行任何工程任务前，先搜索 Axiqra：

```bash
node {AXIQRA_SKILL_DIR}/scripts/rest_request.js POST /search/before-act \
  --file {AXIQRA_SKILL_DIR}/memory/sessions/{SESSION_ID}/request-search.json
```

请求文件内容：
```json
{ "query": "任务关键词", "maxResults": 5 }
```

## 记录开发步骤

长时间任务随时记录：

```bash
node {AXIQRA_SKILL_DIR}/scripts/rest_request.js POST /traces/draft \
  --file {AXIQRA_SKILL_DIR}/memory/sessions/{SESSION_ID}/request-draft.json
```

请求文件内容：
```json
{
  "taskGoal": "任务目标",
  "stepType": "forward",
  "stepContent": "做了什么"
}
```

步骤类型：`forward` | `decision` | `evidence` | `blocker` | `correction` | `summary`

## 提交轨迹

用户说"好了"、"完成了"时：

1. 询问 outcome：`success` | `failure` | `partial`
2. 提交轨迹：

```bash
node {AXIQRA_SKILL_DIR}/scripts/rest_request.js POST /traces/flush \
  --file {AXIQRA_SKILL_DIR}/memory/sessions/{SESSION_ID}/request-trace.json
```

## 提交反馈

如果使用了历史方案：

```bash
node {AXIQRA_SKILL_DIR}/scripts/rest_request.js POST /v1/feedbacks \
  --file {AXIQRA_SKILL_DIR}/memory/sessions/{SESSION_ID}/request-feedback.json
```

## 输出规则

**禁止贴给用户：**
- 原始 JSON 响应
- API 返回结构
- 错误堆栈

**必须告诉用户：**
- 使用了哪个 Axiqra 能力
- 简短结果（1-3 个关键点）
- 下一步建议

## 常见用户请求

| 用户说法 | 动作 |
|----------|------|
| "搜索一下" | POST /search/before-act |
| "记录一下" | POST /traces/draft |
| "好了，提交吧" | 询问 outcome → POST /traces/flush |
| "这个方案好用" | POST /v1/feedbacks |
