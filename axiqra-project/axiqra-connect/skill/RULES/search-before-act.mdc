# Axiqra 行动前搜索规则

> **触发时机**：AI Agent 在执行工程任务前必须先搜索 Axiqra 历史方案

## 触发条件

在以下场景，Agent **必须**在行动前先调用 `axiqra search`：

### 1. 功能实现
- 开始写新功能代码
- 实现新接口/模块
- 引入新依赖/库

### 2. 问题排查
- 遇到错误/异常
- 调试生产问题
- 性能问题排查

### 3. 技术选型
- 选择框架/库
- 选择数据库
- 选择部署方案

### 4. 集成任务
- 接入第三方服务
- 协议集成（MCP/A2A）
- API 对接

### 5. 架构决策
- 数据库设计
- 服务拆分
- 缓存策略
- 消息队列

## 搜索策略

### 精确优先

```
axiqra search "<具体技术> <具体问题>"
例: axiqra search "Cursor MCP 连接超时"
```

### 模糊兜底

```
axiqra search "<通用场景>"
例: axiqra search "MCP 协议集成"
```

### 结构化查询

```
axiqra search --tag java "Redis 连接池"
axiqra search --tag spring "数据库迁移"
axiqra search --level L3 "微服务架构"
```

## 决策规则

### 有方案时

1. 获取方案详情: `axiqra solution get <id>`
2. 评估方案适用性
3. 适用 → 按方案执行
4. 不适用 → 搜索修正词 + 重新搜索

### 无方案时

1. 自行解决
2. 完成后提交轨迹: `axiqra trace submit`
3. 新方案入库后自动通知

## 禁止事项

- ❌ 不搜索就动手实现
- ❌ 不搜索就决定技术选型
- ❌ 不搜索就开始排查问题
- ❌ 不搜索就决定数据库设计
- ❌ 不搜索就决定架构方案

## 例外情况

- 纯文本编辑（如文档修改）
- 代码格式化
- 已有方案明确不适用当前场景

## 验证等级与搜索

搜索结果会显示验证等级（L0-L5）：

| 等级 | 含义 | 使用建议 |
|------|------|---------|
| L0 | 草稿 | 仅参考 |
| L1 | 用户确认 | 需验证 |
| L2 | 有证据 | 可使用 |
| L3 | 可复现 | 优先使用 |
| L4 | 多上下文验证 | 高置信 |
| L5 | 跨上下文验证 | 最高置信 |

> **注意**：`total < 5` 时不显示成功率，显示"数据积累中"

## 风险等级提示

搜索结果会显示风险等级（R0-R4）：

| 等级 | 含义 | 是否需要确认 |
|------|------|--------------|
| R0 | 无风险 | 不需要 |
| R1 | 低风险 | 建议确认 |
| R2 | 中风险 | **必须确认** |
| R3 | 高风险 | **必须人工确认** |
| R4 | 极高风险 | **禁止自动执行** |

## 反馈闭环

搜索结果如果有帮助，请提交反馈：

```bash
# 方案有效
axiqra feedback <invocation_id> worked

# 方案部分有效
axiqra feedback <invocation_id> partial --reason "版本不匹配"

# 方案无效
axiqra feedback <invocation_id> failed --reason "场景不适用"
```

## 关联规则

- `trace-submit.mdc` - 轨迹提交规则
- `feedback.mdc` - 反馈规则
