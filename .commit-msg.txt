chore: address CodeRabbit review comments - MDC propagation, absolute Location URIs, deduplicate LoginResponse

- AsyncConfig: add TaskDecorator (MdcTaskDecorator) to propagate MDC context from
  request threads into async worker threads, so traceId is available in
  AsyncUncaughtExceptionHandler logs
- AuthController: use HttpServletRequest to build absolute Location URIs for 201
  responses; extract buildLoginResponse() helper to deduplicate LoginResponse
  construction in login() and register()
- ConnectController: inject HttpServletRequest and build absolute Location URI
- WorkspaceController: inject HttpServletRequest and build absolute Location URIs
  for create() and addMember()
- Update unit tests and MockMvc test to match new signatures and absolute URIs
